<resources>

    <style name="Theme.Tournament.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.Tournament.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.Tournament.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>